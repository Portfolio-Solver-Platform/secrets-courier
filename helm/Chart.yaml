apiVersion: v2
name: secrets-delivery
description: Delivers secrets
type: application
version: 1.0.0

dependencies:
  - name: external-secrets
    version: 2.5.0
    repository: "https://charts.external-secrets.io"
